class FireDepartment {
    static eliminateFire(x: number, y: number): void {
        console.log(`Eliminating fire at coordinates (${x}, ${y})`);
        // Additional logic to eliminate fire can be implemented here.
    }
}

export default FireDepartment;